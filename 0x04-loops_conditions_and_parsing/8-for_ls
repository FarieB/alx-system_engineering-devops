#!/usr/bin/env bash
# This script lists files in the current directory, showing only the part after the first dash

# Iterate over each file in the current directory (excluding hidden files)
for i in *
do
    echo "$i" | cut -d'-' -f2
done
